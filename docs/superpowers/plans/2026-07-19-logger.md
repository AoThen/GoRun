# Logger 日志文件功能实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development 或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将 `OutputDebugString` 调用替换为文件日志系统，输出到 `{exe_dir}\GoRun.log`

**架构：** 新增 `Logger.h/cpp` 提供 `Log(level, msg)` / `LogW(level, msg)` 函数 + 宏封装，在 App 初始化时自动打开日志文件，Shutdown 时关闭

**技术栈：** C++17, `std::ofstream`, `std::mutex`, `PathUtils::GetExeDir()`

**涉及文件：**
- 创建：`src/utils/Logger.h`, `src/utils/Logger.cpp`
- 修改：`src/app/App.cpp`, `src/core/Storage.cpp`, `src/core/IconCache.cpp`, `src/platform/D3D11Renderer.cpp`, `tests/CMakeLists.txt`

---

### 任务 1：创建 Logger.h 声明文件

**文件：**
- 创建：`src/utils/Logger.h`

- [ ] **步骤 1：创建 Logger.h**

```cpp
#pragma once

#include <string>

namespace mn {

enum class LogLevel {
    Debug,
    Info,
    Error
};

class Logger {
public:
    static void Init();
    static void Shutdown();
    static void Log(LogLevel level, const std::string& msg);
    static void LogW(LogLevel level, const std::wstring& msg);

private:
    static void Write(LogLevel level, const std::string& utf8Msg);
    static void EnsureOpen();
    static void* s_file;  // FILE* 通过 void* 避免暴露 stdio.h
    static bool s_initialized;
};

} // namespace mn

// 便利宏
#define LOG_DEBUG(msg)    ::mn::Logger::Log(::mn::LogLevel::Debug, (msg))
#define LOG_INFO(msg)     ::mn::Logger::Log(::mn::LogLevel::Info, (msg))
#define LOG_ERROR(msg)    ::mn::Logger::Log(::mn::LogLevel::Error, (msg))
#define LOG_DEBUGW(msg)   ::mn::Logger::LogW(::mn::LogLevel::Debug, (msg))
#define LOG_INFOW(msg)    ::mn::Logger::LogW(::mn::LogLevel::Info, (msg))
#define LOG_ERRORW(msg)   ::mn::Logger::LogW(::mn::LogLevel::Error, (msg))
```

---

### 任务 2：创建 Logger.cpp 实现文件

**文件：**
- 创建：`src/utils/Logger.cpp`

- [ ] **步骤 1：创建 Logger.cpp**

```cpp
#include "Logger.h"
#include "utils/PathUtils.h"
#include <cstdio>
#include <ctime>
#include <string>

namespace mn {

void* Logger::s_file = nullptr;
bool Logger::s_initialized = false;

static const char* LevelToString(LogLevel level) {
    switch (level) {
        case LogLevel::Debug: return "DEBUG";
        case LogLevel::Info:  return "INFO";
        case LogLevel::Error: return "ERROR";
        default:              return "UNKNOWN";
    }
}

static std::string GetTimestamp() {
    std::time_t now = std::time(nullptr);
    std::tm tm;
    localtime_s(&tm, &now);
    char buf[32];
    std::strftime(buf, sizeof(buf), "%Y-%m-%d %H:%M:%S", &tm);
    return buf;
}

void Logger::EnsureOpen() {
    if (s_file) return;
    std::wstring logPath = PathUtils::GetExeDir() + L"\\GoRun.log";
    s_file = _wfopen(logPath.c_str(), L"ab");  // append binary
    if (!s_file) {
        // 无法创建日志文件，静默失败
        return;
    }
    // UTF-8 BOM
    fputs("\xEF\xBB\xBF", static_cast<FILE*>(s_file));
    s_initialized = true;
}

void Logger::Init() {
    EnsureOpen();
}

void Logger::Shutdown() {
    if (s_file) {
        fclose(static_cast<FILE*>(s_file));
        s_file = nullptr;
    }
    s_initialized = false;
}

void Logger::Write(LogLevel level, const std::string& utf8Msg) {
    EnsureOpen();
    if (!s_file) return;
    std::string line = "[" + GetTimestamp() + "] [" + LevelToString(level) + "] " + utf8Msg + "\n";
    fputs(line.c_str(), static_cast<FILE*>(s_file));
    fflush(static_cast<FILE*>(s_file));
}

void Logger::Log(LogLevel level, const std::string& msg) {
    Write(level, msg);
}

void Logger::LogW(LogLevel level, const std::wstring& msg) {
    Write(level, StringUtils::WStringToUtf8(msg));
}

} // namespace mn
```

注意：需要包含 `"utils/StringUtils.h"` 因为使用了 `WStringToUtf8`。

修正后的 Logger.cpp：

```cpp
#include "Logger.h"
#include "utils/PathUtils.h"
#include "utils/StringUtils.h"
#include <cstdio>
#include <ctime>
#include <string>

namespace mn {

void* Logger::s_file = nullptr;
bool Logger::s_initialized = false;

static const char* LevelToString(LogLevel level) {
    switch (level) {
        case LogLevel::Debug: return "DEBUG";
        case LogLevel::Info:  return "INFO";
        case LogLevel::Error: return "ERROR";
        default:              return "UNKNOWN";
    }
}

static std::string GetTimestamp() {
    std::time_t now = std::time(nullptr);
    std::tm tm;
    localtime_s(&tm, &now);
    char buf[32];
    std::strftime(buf, sizeof(buf), "%Y-%m-%d %H:%M:%S", &tm);
    return buf;
}

void Logger::EnsureOpen() {
    if (s_file) return;
    std::wstring logPath = PathUtils::GetExeDir() + L"\\GoRun.log";
    s_file = _wfopen(logPath.c_str(), L"ab");
    if (!s_file) {
        return;
    }
    fputs("\xEF\xBB\xBF", static_cast<FILE*>(s_file));
    s_initialized = true;
}

void Logger::Init() {
    EnsureOpen();
}

void Logger::Shutdown() {
    if (s_file) {
        fclose(static_cast<FILE*>(s_file));
        s_file = nullptr;
    }
    s_initialized = false;
}

void Logger::Write(LogLevel level, const std::string& utf8Msg) {
    EnsureOpen();
    if (!s_file) return;
    std::string line = "[" + GetTimestamp() + "] [" + LevelToString(level) + "] " + utf8Msg + "\n";
    fputs(line.c_str(), static_cast<FILE*>(s_file));
    fflush(static_cast<FILE*>(s_file));
}

void Logger::Log(LogLevel level, const std::string& msg) {
    Write(level, msg);
}

void Logger::LogW(LogLevel level, const std::wstring& msg) {
    Write(level, StringUtils::WStringToUtf8(msg));
}

} // namespace mn
```

---

### 任务 3：修改 App.cpp — 初始化 Logger 并替换日志调用

**文件：**
- 修改：`src/app/App.cpp`

- [ ] **步骤 1：在 App::Initialize 开头添加 Logger::Init()**

在 `App::Initialize` 中，`s_instance = this;` 之后立即添加：

```cpp
Logger::Init();
```

需要包含头文件：`#include "utils/Logger.h"`

- [ ] **步骤 2：替换两处 OutputDebugStringW**

位置 1 (line 100):
```cpp
// 替换前：
OutputDebugStringW(L"App: Failed to load config, starting with empty data\n");
// 替换后：
LOG_ERRORW(L"App: Failed to load config, starting with empty data");
```

位置 2 (line 158):
```cpp
// 替换前：
OutputDebugStringW(L"App: Failed to register global hotkey\n");
// 替换后：
LOG_ERRORW(L"App: Failed to register global hotkey");
```

- [ ] **步骤 3：在 Shutdown 中添加 Logger::Shutdown()**

在 `App::Shutdown()` 末尾（`m_hMutex` 释放之前）添加：

```cpp
Logger::Shutdown();
```

---

### 任务 4：修改 Storage.cpp — 替换日志调用

**文件：**
- 修改：`src/core/Storage.cpp`

- [ ] **步骤 1：添加头文件**

在 `Storage.cpp` 的 include 中添加：

```cpp
#include "utils/Logger.h"
```

- [ ] **步骤 2：替换两处异常日志**

位置 1 (lines 154-157):
```cpp
// 替换前：
} catch (const std::exception& e) {
    OutputDebugStringA("Storage::Load failed: ");
    OutputDebugStringA(e.what());
    OutputDebugStringA("\n");
    return false;
}
// 替换后：
} catch (const std::exception& e) {
    LOG_ERROR(std::string("Storage::Load failed: ") + e.what());
    return false;
}
```

位置 2 (lines 158-161):
```cpp
// 替换前：
} catch (...) {
    OutputDebugStringA("Storage::Load failed: unknown error\n");
    return false;
}
// 替换后：
} catch (...) {
    LOG_ERROR("Storage::Load failed: unknown error");
    return false;
}
```

---

### 任务 5：修改 IconCache.cpp — 替换日志调用

**文件：**
- 修改：`src/core/IconCache.cpp`

- [ ] **步骤 1：添加头文件**

在 `IconCache.cpp` 的 include 中添加：

```cpp
#include "utils/Logger.h"
```

- [ ] **步骤 2：替换 OutputDebugStringW**

位置 (line 130):
```cpp
// 替换前：
OutputDebugStringW(L"IconCache: Failed to save icon\n");
// 替换后：
LOG_ERRORW(L"IconCache: Failed to save icon");
```

---

### 任务 6：修改 D3D11Renderer.cpp — 替换日志调用

**文件：**
- 修改：`src/platform/D3D11Renderer.cpp`

- [ ] **步骤 1：添加头文件**

在 `D3D11Renderer.cpp` 的 include 中添加：

```cpp
#include "utils/Logger.h"
```

- [ ] **步骤 2：替换字体加载日志**

位置 (lines 186-188):
```cpp
// 替换前：
OutputDebugStringA("Font loaded: ");
OutputDebugStringA(path);
OutputDebugStringA("\n");
// 替换后：
LOG_INFO(std::string("Font loaded: ") + path);
```

- [ ] **步骤 3：替换字体加载失败日志**

位置 (line 194):
```cpp
// 替换前：
OutputDebugStringA("Failed to load Chinese font, using default\n");
// 替换后：
LOG_INFO("Failed to load Chinese font, using default");
```

---

### 任务 7：修改 tests/CMakeLists.txt — 添加 Logger.cpp 到测试构建

**文件：**
- 修改：`tests/CMakeLists.txt`

- [ ] **步骤 1：添加 Logger.cpp 到 TEST_UTIL_SOURCES**

```cmake
# 在 TEST_UTIL_SOURCES 中添加
"${CMAKE_SOURCE_DIR}/src/utils/Logger.cpp"
```

---

### 任务 8：验证构建

- [ ] **步骤 1：CMake 配置**

```bash
cmake -B build -A x64
```

- [ ] **步骤 2：编译确认**

```bash
cmake --build build --config Release
```

预期：无编译错误，`GoRun.exe` 生成成功